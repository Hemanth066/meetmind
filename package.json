{
  "name": "meetmind",
  "version": "1.0.0",
  "description": "AI-powered video meeting platform with ML analytics",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && pip install -r ml-service/requirements.txt",
    "dev": "npm run dev --prefix backend",
    "dev:ml": "cd ml-service && python -m uvicorn main:app --reload --port 8001",
    "start": "npm start --prefix backend"
  },
  "keywords": ["webrtc", "meetings", "ai", "analytics"],
  "license": "MIT"
}
